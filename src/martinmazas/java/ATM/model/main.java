package martinmazas.java.ATM.model;

public class main {
    public static void main(String args[]) throws ATMException {
//        DerbyDBModel model = new DerbyDBModel();

        // Account table

//        model.createTable("Account", "(accountId int NOT NULL PRIMARY KEY, " +
//                "dollarAccountId varchar(256), shekelAccountId varchar(256), euroAccountId varchar(256))");
//
//
//         //DollarAccount Table
//        model.createTable("DollarAccount", "(dollarId int NOT NULL PRIMARY KEY ,accountId int " +
//                "NOT NULL, date DATE NOT NULL, action int NOT NULL, balance int NOT NULL, " +
//                "FOREIGN KEY(accountId) REFERENCES Account(accountId))");
//
//          // ShekelAccount Table
//        model.createTable("ShekelAccount", "(shekelId int NOT NULL PRIMARY KEY, accountId int " +
//                "NOT NULL, date DATE NOT NULL, action int NOT NULL, balance int NOT NULL, " +
//                "FOREIGN KEY(accountId) REFERENCES Account(accountId))");
//
//         // EuroAccount Table
//        model.createTable("EuroAccount", "(euroId int NOT NULL PRIMARY KEY, accountId int " +
//                "NOT NULL, date DATE NOT NULL, action int NOT NULL, balance int NOT NULL, " +
//                "FOREIGN KEY(accountId) REFERENCES Account(accountId))");
//
//        //  Users Table
//        model.createTable("Users", "(userId int NOT NULL PRIMARY KEY, name varchar(255) NOT NULL, " +
//                "accountId int NOT NULL UNIQUE, pin varchar(256) NOT NULL, FOREIGN KEY (accountId) REFERENCES" +
//                " Account(accountId))");
    }
}
